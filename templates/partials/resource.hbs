= {{name}} [{{group}}/{{version}}]

{{#each definition.propertiesByPath}}
{{#with (lookup ../definition.flatSpec @key)}}

{{#if (notRoot @key)}}
.{{@key}}
{{/if}}

Description::
  {{escapeMarkup description}}

Type::
  `{{type}}`
{{/with}}

{{#unless (notRoot @key)}}
== Specification
{{/unless}}

{{#if (hasChildren this)}}
[cols="1,1,1",options="header"]
|===
| Property | Type | Description

{{#each (flatPropertiesSliceForTable ../definition.flatSpec this)}}
| `{{truncatePath property @../key}}`
| {{type}}
| {{{escapeMarkup description}}}

{{/each}}
|===
{{/if}}

{{/each}}

== Related API objects

{{#each definition.relatedDefinitions}}
{{#with (findDefinitionByKey .)}}

=== {{name}} [{{group}}/{{version}}]

{{#each propertiesByPath}}
{{#with (lookup ../flatSpec @key)}}

{{#if (notRoot @key)}}
.{{@key}}
{{/if}}

Description::
  {{escapeMarkup description}}

Type::
  `{{type}}`
{{/with}}

{{#if (hasChildren this)}}
[cols="1,1,1",options="header"]
|===
| Property | Type | Description

{{#each (flatPropertiesSliceForTable ../flatSpec this)}}
| `{{truncatePath property @../key}}`
| {{type}}
| {{{escapeMarkup description}}}

{{/each}}
|===
{{/if}}

{{/each}}
{{/with}}
{{/each}}

//== Operations

{{!--#each operationCategories--}}

{{!-->operationCategory .--}}

{{!--/each--}}

{{!--/with--}}
